#!/bin/sh

# Usage: $0
# change login shell to zsh

requires() {
   command -v "$@" > /dev/null && return 0
   printf 'Cannot find %s\n' "$*" >&2
   exit 1
}

requires chsh
requires zsh
chsh -s "$(which zsh)"
