#!/bin/sh

# Usage: $0 [FILE...]
# grep what is *NOT* PRINTABLE ASCII

if echo | grep -q --color=auto '' 2>/dev/null
   then COLOR=--color=auto
   else COLOR=''
fi

LC_ALL=C grep $COLOR '[^ -~]' "$@"
